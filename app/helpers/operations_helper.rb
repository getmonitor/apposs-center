# coding: utf-8
module OperationsHelper

#  def actions operation
#    actions = []
#    actions << link_to('激活', enable_operation_path(operation.id), :remote => true, :method => :put ) if operation.hold?
#    actions << link_to(
#      '撤销', 
#      clear_operation_path(operation.id), 
#      :remote => true, :method => :put, :handle => true
#    ) if Operation::STATE_COULD_BE_CLEAR.include?(operation.state)
#    raw actions.join("\n")
#  end

end

